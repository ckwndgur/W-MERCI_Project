<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="nsllab.merci_android_updated">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.location.gps" android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDrJ8Leof8qmNNfVRubIrNd1gF9ROk7utE" />

        <activity android:name=".InitialActivity" android:theme="@style/BlankTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LangActivity" android:theme="@style/BlankTheme"></activity>
        <activity android:name=".LoginActivity" android:theme="@style/BlankTheme"></activity>
        <activity android:name=".NetworkSettingsActivity" android:theme="@style/AppTheme" android:label="@string/network_settings_label"></activity>
        <activity android:name=".HomeActivity" android:theme="@style/BlankTheme"></activity>
        <activity android:name=".EditAssessmentActivity" android:theme="@style/BlankTheme"></activity>
        <activity android:name=".EditAssessmentExtraFieldsActivity" android:theme="@style/BlankTheme"></activity>
    </application>

</manifest>
